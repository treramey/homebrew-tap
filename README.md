# treramey Homebrew tap

Homebrew formulas for Trevor Ramey's command-line tools.

## Install

```sh
brew install treramey/tap/drag
brew install treramey/tap/graduate
```

The Graduate formula is added automatically when its first release is published.
